{
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "jest": "^29.7.0"
  },
  "scripts": {
    "test": "jest",
    "validate:data": "node scripts/validate-all.js",
    "fetch:browse": "node scripts/fetch-directory.js"
  }
}
