# -----------------------------
# Repomix ignore for DateMeDirectory
# Goal: include code + docs + schemas, exclude data dumps + environments + binaries
# -----------------------------

# Git internals
.git/**

# Python / node environments and caches
.venv/**
venv/**
__pycache__/**
**/__pycache__/**
.pytest_cache/**
.mypy_cache/**
.ruff_cache/**
*.pyc

node_modules/**
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml

# Playwright / browser binaries (often huge)
**/playwright/**

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/**
.idea/**

# -----------------------------
# Data
# -----------------------------
# Only ignore the archive folder under data
data/archive/**

# Common massive formats from scraping/exports (ignore ONLY under data/)
data/**/*.html
data/**/*.htm
data/**/*.ndjson
data/**/*.jsonl
data/**/*.parquet
data/**/*.sqlite
data/**/*.db
data/**/*.tsv
data/**/*.csv
data/**/*.json

# (Optional) If you keep schemas under data/, allow them explicitly
!data/schemas/**
!data/schemas/**

# If you want to include a tiny curated dataset, allow it explicitly
!data/sample/**
!data/sample/**

# -----------------------------
# Allowlist important repo JSON configs (keep these even though we ignore data/**/*.json)
# -----------------------------
!package.json
!tsconfig.json
!eslint*.json
!prettier*.json
!*.schema.json
!schemas/**/*.json
!schemas/**/*.schema.json

# -----------------------------
# Repomix outputs (do not pack the pack)
# -----------------------------
repomix-output.*
**/repomix-output.*

# Build / dist outputs (if you have any)
dist/**
build/**
coverage/**
